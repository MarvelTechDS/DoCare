<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.marveltech.docare">
<uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:icon="@drawable/docareicon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/docareicon"
        android:supportsRtl="true"
        android:theme="@style/Theme.DoCare">
        <activity android:name=".OTPActivity" android:theme="@style/Theme.Design.NoActionBar" android:noHistory="true"/>
        <activity android:name=".HomeActivity2" android:noHistory="true"/>
        <activity android:name=".LoginActivity"  android:theme="@style/Theme.Design.NoActionBar" android:noHistory="true"/>
        <activity android:name=".MainActivity"  android:theme="@style/Theme.Design.NoActionBar" android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>